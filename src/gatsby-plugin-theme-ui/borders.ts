import { Theme } from "theme-ui";

const borders: Theme["borders"] = {
  default: "2px solid",
  one: "1px solid",
  /* button: "2px solid var(--theme-ui-colors-shadow-default)", */
};

export default borders;
